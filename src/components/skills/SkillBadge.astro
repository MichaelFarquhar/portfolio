---
interface Props {
    src: string;
    alt: string;
    group: string; // Tailwind hover group name
}

const { src, alt, group } = Astro.props;
---

<img class="transition ease-in-out" data-type={group} src={src} alt={alt} title={alt} />
